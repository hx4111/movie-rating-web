export type SiteConfig = typeof siteConfig;

export const siteConfig = {
  name: "Movie rating",
  description: "A movie rating web application use Nextjs and NextUI",
};

export const urlBase = "http://movie-rating-example/"
